---
slug: vue-jsonp
title: vue-jsonp
authors:
  name: Kiyan-a
  title: 唔洗讲ye，哈哈哈哈哈！
  url: https://github.com/Kiyan-a
  image_url: https://avatars.githubusercontent.com/u/86712674?v=4
tags: [Vue, 请求, Jsonp]
---

在 vue 中使用 vue-jsonp (jsonp 跨域)

```shell
npm install vue-jsonp
```

局部引入：

```js
import { jsonp } from 'vue-jsonp'


created() {
    jsonp(
     'https://news.sina.com.cn/project/fymap/ncp2020_full_data.json,{
 	 ...参数,
 	 callback:'jsoncallback',//值需要与window.jsoncallback一样
	}).then(() => {
      // 处不会读取到数据，数据在 window.jsoncallback 中读取
    });
  },

  mounted() {
    window.jsoncallback = data => {
      console.log(data);
    };
  },
```
